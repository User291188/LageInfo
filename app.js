(()=>{const C=window.LAGEINFO_CONFIG,S=window.LageSources;const map=L.map('map',{zoomControl:false}).setView([51.1,10.3],6);L.control.zoom({position:'bottomleft'}).addTo(map);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:18,attribution:'© OpenStreetMap'}).addTo(map);const layers={missions:L.layerGroup().addTo(map),quakes:L.layerGroup().addTo(map),fires:L.layerGroup().addTo(map)};C.regions.forEach(r=>L.circleMarker([r.lat,r.lon],{radius:7,weight:2,fillOpacity:.8}).bindPopup(`<b>${r.name}</b><br>${r.type}`).addTo(layers.missions));let mode='de',items=[];const feed=document.querySelector('#feed'),search=document.querySelector('#search'),count=document.querySelector('#count'),status=document.querySelector('#status');const esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function render(){const q=search.value.trim().toLowerCase();const types=mode==='de'?['warn','police']:mode==='world'?['news','quake','fire']:[];const rows=items.filter(x=>types.includes(x.type)).filter(x=>!q||[x.area,x.source,x.title,x.text].join(' ').toLowerCase().includes(q)).sort((a,b)=>new Date(b.date||0)-new Date(a.date||0)).slice(0,100);count.textContent=rows.length;feed.innerHTML=rows.length?rows.map(x=>`<article class="item"><small>${esc(x.area)} · ${esc(x.source)}</small><b><div>${esc(x.title)}</div></b><p>${esc(x.text)}</p>${x.url?`<a href="${esc(x.url)}" target="_blank" rel="noopener">Quelle öffnen</a>`:''}</article>`).join(''):`<div class="empty">${mode==='air'?'Luftlage wird als eigenes Modul angebunden.':'Keine passenden Meldungen.'}</div>`}function addQuakes(rows){layers.quakes.clearLayers();rows.filter(x=>Number.isFinite(+x.lat)&&Number.isFinite(+x.lon)).forEach(x=>L.circleMarker([+x.lat,+x.lon],{radius:Math.max(3,Number((x.title.match(/M ([\d.]+)/)||[])[1])||3),weight:1,fillOpacity:.65}).bindPopup(`<b>${esc(x.title)}</b>`).addTo(layers.quakes))}function addFires(rows){layers.fires.clearLayers();rows.filter(x=>Number.isFinite(+x.lat)&&Number.isFinite(+x.lon)).slice(0,1500).forEach(x=>L.circleMarker([+x.lat,+x.lon],{radius:3,weight:0,fillOpacity:.65}).bindPopup(`<b>${esc(x.title)}</b><br>${esc(x.text)}`).addTo(layers.fires))}function dedupe(rows){return [...new Map(rows.map(x=>[x.id||`${x.source}-${x.title}-${x.date}`,x])).values()]}async function load(){status.textContent='Vercel Live-Daten werden geladen …';const tasks=(C.ninaARS||[]).map(ars=>S.nina(ars).catch(()=>[]));tasks.push(S.quakes().catch(()=>[]),S.gdelt(C.gdeltQuery).catch(()=>[]),S.firms('missions',1).catch(()=>[]));const all=dedupe((await Promise.all(tasks)).flat());items=all;addQuakes(all.filter(x=>x.type==='quake'));addFires(all.filter(x=>x.type==='fire'));status.textContent=`VERCEL LIVE · ${items.length} Datensätze · ${new Date().toLocaleTimeString('de-DE')}`;render()}search.oninput=render;document.querySelectorAll('.chips button').forEach(b=>b.onclick=()=>{search.value=b.textContent;render()});document.querySelectorAll('.tabs button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tabs button').forEach(x=>x.classList.remove('active'));b.classList.add('active');mode=b.dataset.view;if(mode==='de')map.setView([51.1,10.3],6);else if(mode==='world')map.setView([35,25],3);render()});document.querySelectorAll('[data-layer]').forEach(c=>c.onchange=e=>{const l=layers[e.target.dataset.layer];if(l)e.target.checked?l.addTo(map):map.removeLayer(l)});setInterval(()=>document.querySelector('#clock').textContent=new Date().toLocaleTimeString('de-DE'),1000);load();setInterval(load,300000)})();