export default async function handler(req,res){res.setHeader('Access-Control-Allow-Origin','*');res.setHeader('Cache-Control','s-maxage=180, stale-while-revalidate=300');try{const r=await fetch('https://earthquake.usgs.gov/earthquakes/feed/v1.0/summary/2.5_day.geojson');if(!r.ok)throw new Error(`USGS ${r.status}`);const g=await r.json();const items=(g.features||[]).map(f=>({id:f.id,source:'USGS',area:f.properties.place||'Erdbeben',title:`M ${f.properties.mag} · ${f.properties.place||''}`,text:'Erdbeben der letzten 24 Stunden ab Magnitude 2,5',date:new Date(f.properties.time).toISOString(),type:'quake',lat:f.geometry.coordinates[1],lon:f.geometry.coordinates[0],url:f.properties.url}));res.status(200).json({ok:true,count:items.length,items})}catch(e){res.status(502).json({ok:false,error:e.message,items:[]})}}