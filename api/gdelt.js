export default async function handler(req,res){res.setHeader('Access-Control-Allow-Origin','*');res.setHeader('Cache-Control','s-maxage=180, stale-while-revalidate=300');const query=String(req.query.q||'(Bundeswehr OR "German military" OR NATO) (Jordan OR Kosovo OR Lithuania OR Iraq OR Turkey)').slice(0,500);try{const u=`https://api.gdeltproject.org/api/v2/doc/doc?query=${encodeURIComponent(query)}&mode=ArtList&maxrecords=50&format=json&sort=HybridRel`;const r=await fetch(u);if(!r.ok)throw new Error(`GDELT ${r.status}`);const d=await r.json();const items=(d.articles||[]).map((a,i)=>({id:a.url||`gdelt-${i}`,source:`GDELT · ${a.domain||''}`,area:a.sourcecountry||'International',title:a.title||'Meldung',text:a.seendate?`Erfasst: ${a.seendate}`:'',date:a.seendate||'',type:'news',url:a.url}));res.status(200).json({ok:true,count:items.length,items})}catch(e){res.status(502).json({ok:false,error:e.message,items:[]})}}